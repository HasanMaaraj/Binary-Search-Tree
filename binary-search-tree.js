const treeFactory = (array) => {
    let root = null;
    const nodeFactory = () => {
        return {
            leftNode: null,
            rightNode: null,
            data: null,

        }
    }
}